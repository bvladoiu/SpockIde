Dsl style programming is top priority, not the kotlin naming convention. 
e.g. instead of writing methods buildPage(..){..} and buildSection(..){...},
opt for page(..){..} and section(..){..}
Add any newly created files to git. but do not commit. 